Backend in Terraform is used to store the Terraform state file (terraform.tfstate).
Terraform keeps track of all resources it creates in a state file. By default, this file is stored locally on your machine, but in a team environment, we store it remotely using a backend.
